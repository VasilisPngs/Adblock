{
  "name": "adblock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run lists && npx wrangler d1 migrations apply DB --remote && npx wrangler deploy",
    "lists": "node scripts/build-blocklist.mjs",
    "db:migrate": "wrangler d1 migrations apply DB --local",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
